import { Safeurl.Pipe.TsPipe } from './safeurl.pipe.ts.pipe';

describe('Safeurl.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new Safeurl.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
